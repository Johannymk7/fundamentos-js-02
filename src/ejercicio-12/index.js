// Escribe tu código aquí:
// Declarar la variable fuera del bloque
let variableBloque;

if (true) {
  // Asignar un valor dentro del bloque
  variableBloque = 'Variable dentro del bloque';
}

// Acceder a la variable fuera del bloque
console.log(variableBloque); // Generará un error, variableBloque no está definida fuera del bloque